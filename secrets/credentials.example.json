{
  "_comment": [
    "Copy to credentials.json and fill in. This file is gitignored — it never leaves",
    "a machine except by a channel you choose (password manager, encrypted drive).",
    "",
    "The five *_MCP_BASIC values are base64 of 'user:app-password' for each WordPress",
    "site. Read the live ones off the Mac with:",
    "  python3 -c \"import json;print(json.dumps(json.load(open('$HOME/.claude/settings.json'))['env'],indent=2))\"",
    "",
    "GOOGLE_ADS_DEVELOPER_TOKEN is in the Mac Keychain, not in settings.json. Read it with:",
    "  security find-generic-password -a claude -s google-ads-dev-token -w"
  ],
  "env": {
    "SERENITE_MCP_BASIC": "",
    "ELEMENTOR_MCP_BASIC": "",
    "EMADA1_MCP_BASIC": "",
    "MAKETHISQUICK_MCP_BASIC": "",
    "LIPGLOSSLUXE_MCP_BASIC": ""
  },
  "googleAds": {
    "GOOGLE_ADS_DEVELOPER_TOKEN": ""
  }
}
